- name: "Copy: eggroll keys (exchange)"
  copy:
    src: "keys/{{ item.src }}"
    dest: "{{dbase}}/{{pname}}/keys/{{ item.dest }}"
  with_items:
  - { src: 'exchange/exchange-ca.pem', dest: 'exchange-ca.pem' }
  - { src: 'exchange/exchange-server.pem', dest: 'exchange-server.pem' }
  - { src: 'exchange/exchange-server.key', dest: 'exchange-server.key' }
  - { src: 'exchange/exchange-client-ca.pem', dest: 'exchange-client-ca.pem' }
  - { src: 'exchange/exchange-client-client.pem', dest: 'exchange-client-client.pem' }
  - { src: 'exchange/exchange-client-client.key', dest: 'exchange-client-client.key' }
  when: "ssl_roles|length == 2 and 'exchange' in ssl_roles and rollsite.get('server_secure', False) == True and rollsite.get('client_secure', False) == True"

- name: "update({{deploy_mode}}): {{pbase}}/{{pname}}/eggroll/conf/eggroll.properties(exchange)"
  template: src=eggroll-exchange.properties.jinja dest={{pbase}}/{{pname}}/eggroll/conf/eggroll.properties

- name: "update({{deploy_mode}}): {{pbase}}/{{pname}}/eggroll/conf/route_table_tmp.json(exchange)"
  template: src=route_table.json.jinja dest={{pbase}}/{{pname}}/eggroll/conf/route_table_tmp.json

- name: "new({{deploy_mode}}): {{pbase}}/{{pname}}/eggroll/bin/json-replace.sh"
  template: src=json-replace.sh.jinja dest={{pbase}}/{{pname}}/eggroll/bin/json-replace.sh

- name: "new({{deploy_mode}}): {{pbase}}/{{pname}}/bin/init_env.sh"
  template: src=exchange-init_env.sh.jinja dest={{pbase}}/{{pname}}/bin/init_env.sh

- name: "check({{deploy_mode}}): route table json format"
  shell: /bin/bash {{pbase}}/{{pname}}/eggroll/bin/json-replace.sh
  register: json_replace

- debug: var=json_replace['stdout_lines']

- name: "output({{deploy_mode}}): check json bad result"
  fail:
    msg: "please check json"
  when: json_replace.stdout.find('check_json_pass') == -1

- name: "untar({{deploy_mode}}): osx.tar.gz(exchange)"
  unarchive:
    src: "osx.tar.gz"
    dest: "{{pbase}}/{{pname}}"

- name: "update({{deploy_mode}}): {{pbase}}/{{pname}}/osx/conf/broker/route_tebale.json"
  template: src=route_table.json.jinja dest={{pbase}}/{{pname}}/osx/conf/broker/route_table.json
 
- name: "update({{deploy_mode}}): {{pbase}}/{{pname}}/osx/conf/broker/broker.properties(exchange)"
  template: src=broker.properties.jinja dest={{pbase}}/{{pname}}/osx/conf/broker/broker.properties

- name: "update({{deploy_mode}}): {{cbase}}/supervisord/supervisord.d/{{pname}}-osx.conf(exchange)"
  template: src=supervisor.d/osx.conf.jinja dest={{cbase}}/supervisord/supervisord.d/{{pname}}-osx.conf
  vars:
    role: osx
    service: "{{supervisord['service']}}"
  notify:
  - reload {{pname}}-osx
  - restart {{pname}}-osx

- name: "flush_handlers" 
  meta: flush_handlers

- name: reload {{pname}}-osx
  shell: /bin/bash {{cbase}}/supervisord/service.sh update {{pname}}-osx

- name: restart {{pname}}-osx
  shell: /bin/bash {{cbase}}/supervisord/service.sh restart {{pname}}-osx

- name: "wait({{deploy_mode}})): open port {{osx.get('httpPort',9370)}}( {{deploy_role}} )(exchange)"
  wait_for:
    host: "{{ansible_ssh_host}}"
    port: "9370"
    delay: 0
    state: started
    timeout: 120
  register: waitfor_fate_osx_http_host
  when: ansible_ssh_host in rollsite.get('ips',[]) 

- name: "check({{deploy_mode}})): check fate_osx service( {{deploy_role}} )(exchange)"
  debug: msg="******start fate_osx successfully******"
  when: "waitfor_fate_osx_http_host.get('state') == 'started'"

- name: "rm eggroll"
  shell: rm -rf {{pbase}}/{{pname}}/eggroll
